package ava.util;

/**
 * Abstract class for connecting to services on the internet
 * 
 * @author dbryan (dylanmbryan@gmail.com)
 *
 */
public class WebConnection {

	/**
	 * Opens a connection with the target device at the given IP through the
	 * given port
	 * 
	 * @param target
	 *            the target devices local IP address
	 * @param port
	 *            the port to connect on
	 */
	public void buildConnection(String target, String port, String username, String pw) {

	}

}
